# Terraform state — never commit
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform/
.terraform.lock.hcl

# Sensitive values — never commit
terraform.tfvars
*.tfvars
!example.tfvars

# Lambda placeholder
lambda_placeholder.zip
lambda_placeholder.js

# OS files
.DS_Store
*.swp

# Crash logs
crash.log
example.tfvars
terraform.tfvars
